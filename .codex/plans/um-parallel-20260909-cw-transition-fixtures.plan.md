---
name: um-parallel-20260909-cw-transition-fixtures
overview: "Prepare the exact transition test and immutable historical/customized fixture definitions against the frozen transaction interface while source owners work. Current and next candidate bytes are supplied later."
todos:
  - id: cw-transition-fixtures-deliver
    content: "Prepare the exact transition test and immutable historical/customized fixture definitions against the frozen transaction interface while source owners work. Current and next candidate bytes are supplied later."
    status: pending
  - id: cw-transition-fixtures-prove
    content: "Produce and verify: Executable old-to-current and current-to-next success/failure cases with explicit byte-diff, no-op and rollback assertions; fixture preparation is not acceptance."
    status: pending
isProject: false
---

# Prepare real-cohort update and failure fixtures

## Execution Notes

Tracking: `modernjs-cdhz.29.98`. Mode: **test preparation**. Read [the parallel program](um-parallel-20260909-overview.md) and [the unchanged acceptance contract](um-zero-20260909-overview.md). This is a deferred execution plan; creating it does not perform its tasks.

Prepare the exact transition test and immutable historical/customized fixture definitions against the frozen transaction interface while source owners work. Current and next candidate bytes are supplied later.

Required output: Executable old-to-current and current-to-next success/failure cases with explicit byte-diff, no-op and rollback assertions; fixture preparation is not acceptance.

## Constraints

You are not alone in the codebase. Preserve other owners’ changes. Every worker is a leaf; only the primary agent schedules new nodes. Current planning authorization grants no framework changes or release.

Reserved file scope:

- `packages/toolkit/ultramodern-create/tests/migrate-real-cohort-transitions.test.ts`

Out of scope: No live consumer changes in this node, public release, custom port-remapping config, independent release/cohort owner or rewriting generated apps.

Exact new destinations, source-barrel transfers and consumed interface deliveries must be recorded before writes. An audit report containing unresolved rows is not a completed route gate. Required upstream seams become explicit per-seam graph nodes/edges; an unmerged proposal cannot satisfy a landing prerequisite. Revalidate the exact selection after any expansion.

Keep fixed audited identity and full canonical scope, the complete-PR 20-line cap per audited file, required same-PR ledger and native extension ownership. No allowance reset, hidden imports, marker/identity renaming, app shim, copied framework wrapper, generated-file hand edit or hook bypass.

## Operator Guidance

Owner: `cw-transition-fixtures`. Completion updates this plan and its exact br issue only after evidence is reviewed. Never mark a task complete because it is queued, drafted, or blocked.

Dependencies: [cw-update-contract](um-parallel-20260909-cw-update-contract.plan.md), [cw-historical-fixtures](um-parallel-20260909-cw-historical-fixtures.plan.md)

Resource locks: node-private mutable workspace and artifacts; apply the program’s host/build resource policy.

Verification: Prove meaningful injected ownership/cohort/rollback failures are detected in private fixtures; record missing future candidate bytes without inventing success.

Stop condition: Prepared executable cases and self-check evidence complete this node; absent future candidate bytes do not block preparation. Two distinct real current/next cohorts are mandatory only for gate-update-acceptance.

Dispatch ready nodes continuously, up to 40 active leaf workers under the resolved 50-thread budget. Use immediate per-node handoffs; do not wait for an entire conceptual wave. The primary owns the shared integration service and exact source/receipt reconciliation.
