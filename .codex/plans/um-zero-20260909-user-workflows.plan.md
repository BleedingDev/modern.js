---
name: um-zero-20260909-user-workflows
overview: "Specify simple native usage and measurable update outcomes. Usage/update contract and baseline measurements; private fixture definitions, no live consumer writes."
todos:
  - id: zero-user-workflows-journeys
    content: "Record the current create, install, add-vertical, add-shell, API, check, build, Node/workerd, framework-update and rollback journeys using existing public commands and representative fresh and customized consumers."
    status: pending
  - id: zero-user-workflows-acceptance
    content: "Freeze testable outcomes: one documented supported update entry, zero manual source/config repair on the supported success matrix, zero generated TS/JS or deployment-config changes for same-contract updates, and zero copied framework runtime/checker implementations in fresh apps."
    status: pending
  - id: zero-user-workflows-metrics
    content: "Record changed consumer files, generated artifacts, manual interventions, command count, duplicate validation executions, implementation LOC and relevant time/RSS baselines; distinguish real removal from relocation and test growth."
    status: pending
isProject: false
---

# Specify simple native usage and measurable update outcomes

## Execution Notes

Tracking: `modernjs-cdhz.3`. Parent planning program: `modernjs-cdhz`. **Planning only: every implementation todo remains pending.** No subagents are launched by this plan. Read [the program contract](um-zero-20260909-overview.md) and [initial ownership coverage](notes/um-zero-20260909/ownership-coverage.md).

Reuse the accepted simplification review P1-P8 and the completed PR49 work. Keep the shell-only default, native Effect APIs, TanStack routing, React/Tailwind/i18n, multi-shell composition, independent delivery units and Node/Cloudflare. A same-contract update may update dependency manifests, the lockfile and narrowly derived release identity data; it must not reconstruct authored source or topology. Genuine public/schema changes use the explicit migration path.

## Constraints

Own: Usage/update contract and baseline measurements; private fixture definitions, no live consumer writes.

Keep AGENTS.md Rules 1–5, CONTEXT.md and ADR-0019. Additive fork behavior belongs in genuine fork-owned packages; audited identities retain their full-PR 20-line cap and required same-PR ledger. No allowance reset, scope narrowing, hidden import indirection, generated-output edit, app shim or hook bypass. Current planning permission does not authorize implementation, upstream submission, merge or release.

## Operator Guidance

Owner: **Consumer workflow owner**. You are not alone in the codebase: preserve other owners' work and do not edit outside your reservation. Shared manifest, export, lockfile and ledger changes go to integration owners. At execution, update these todo statuses and the br issue from actual evidence; never mark future work completed merely because its plan exists.

Dependencies: none; this is an independent root lane after execution authorization.

Verification and exit: Signed-off fixture/metric contract with exact starting revisions and commands; no invented percentage savings. Separately identify upstream framework refreshes and consumer cohort updates.

Launch successors when their actual prerequisites finish. Do not impose a global wave or two-agent cap. Retain exact plan selection/dependencies and graph ID when later running plan-graph/subagent-graph/helm. Upstream wait states and shared ports are real constraints; elapsed time is not approval.
