---
name: um-parallel-20260909-audit-build-route-assets
overview: "Keep route manifest construction, worker/server route discovery and RSC-off behavior together because they describe the same compiled route artifacts. Extract genuine additions while treating RouterPlugin and analyze helpers as audited upstream identities."
todos:
  - id: audit-build-route-assets-reconcile
    content: "Reconcile every reserved identity and current import against the pinned execution inventory; prove audited/fork ownership, callers and complete-PR capacity without changing source."
    status: pending
  - id: audit-build-route-assets-route
    content: "Prove a behavior-preserving lawful route and only the native interfaces this shard consumes. Prepare a bounded seam packet for unresolved upstream needs; do not treat an unknown route as approved."
    status: pending
  - id: audit-build-route-assets-grant
    content: "Obtain the integration owner’s exact source/destination reservation and accepted peer interface contracts. Release only this shard when all write prerequisites are real; unresolved rows remain blocked and are registered with explicit dependency edges."
    status: pending
isProject: false
---

# Audit: Route compilation, asset manifests and disabled-RSC runtime

## Execution Notes

Tracking: `modernjs-cdhz.29.47`. Mode: **read-only**. Read [the parallel program](um-parallel-20260909-overview.md) and [the unchanged acceptance contract](um-zero-20260909-overview.md). This is a deferred execution plan; creating it does not perform its tasks.

Keep route manifest construction, worker/server route discovery and RSC-off behavior together because they describe the same compiled route artifacts. Extract genuine additions while treating RouterPlugin and analyze helpers as audited upstream identities.

Required output: Per-identity legal route, caller/seam evidence, complete-PR capacity and one exact source/destination ownership packet for build-route-assets.

Read-only input identities:

- `packages/cli/builder/src/shared/rsc/rscDisabledClientBrowserRuntime.ts`
- `packages/cli/builder/src/shared/rsc/rscDisabledClientEdgeRuntime.ts`
- `packages/cli/builder/src/shared/rsc/rscDisabledClientNodeRuntime.ts`
- `packages/cli/builder/src/shared/rsc/rscDisabledRuntime.ts`
- `packages/cli/builder/src/shared/rsc/rscDisabledRuntimeError.ts`
- `packages/cli/builder/src/shared/rsc/rscDisabledServerEdgeRuntime.ts`
- `packages/cli/builder/src/shared/rsc/rscDisabledServerNodeRuntime.ts`
- `packages/cli/builder/tests/rscDisabledRuntime.test.ts`
- `packages/solutions/app-tools/src/builder/shared/bundlerPlugins/RouterPlugin.ts`
- `packages/solutions/app-tools/src/plugins/analyze/getServerRoutes.ts`
- `packages/solutions/app-tools/src/plugins/analyze/isDefaultExportFunction.ts`
- `packages/solutions/app-tools/tests/builder/routerPlugin.test.ts`

Local seam prerequisites:

- Audit: none; this shard independently identifies its legal routes and required hooks.

- Writes: this shard's legal-route audit; runtime-router route-manifest/native-router consumer contract only for changed artifact fields, and runtime-ssr RSC/worker entry contract only for changed runtime entry resolution. An unavailable native builder hook blocks that specific integration until a genuine upstream proposal lands, not unrelated audits.

## Constraints

You are not alone in the codebase. Preserve other owners’ changes. Every worker is a leaf; only the primary agent schedules new nodes. Current planning authorization grants no framework changes or release.

No framework source write grant. Evidence uses a node-private file or artifact root assigned by the operator.

Prospective destinations (not write grants):

- Prospective fork module packages/solutions/app-tools-extensions/src/route-build/ and its route-build tests/fixtures; this worker alone owns new paths in this subtree.

- Prospective fork module packages/solutions/app-tools-extensions/src/route-build/rsc-disabled/ for the RSC runtime family; no runtime shims in consumer apps.

Out of scope: Source changes, shared manifests/lockfile/ledger/allowlists, upstream submission and other shards.

Exact new destinations, source-barrel transfers and consumed interface deliveries must be recorded before writes. An audit report containing unresolved rows is not a completed route gate. Required upstream seams become explicit per-seam graph nodes/edges; an unmerged proposal cannot satisfy a landing prerequisite. Revalidate the exact selection after any expansion.

Keep fixed audited identity and full canonical scope, the complete-PR 20-line cap per audited file, required same-PR ledger and native extension ownership. No allowance reset, hidden imports, marker/identity renaming, app shim, copied framework wrapper, generated-file hand edit or hook bypass.

Shared-owner handoffs:

- Runtime-router and runtime-ssr owners receive the exact route-manifest/public-path and RSC entry contracts; share schemas or proposal patches without editing their source.

- Any builder plugin registration site outside the listed source identities goes to its designated owner; no second writer or fork import introduced into an upstream caller.

- Integrator owns existing and prospective package.json manifests and public export maps; provide exact proposed patches without editing those files.

- Integrator owns pnpm-lock.yaml, release cohort/version metadata, FORK-DIVERGENCE.md and scripts/ultramodern-boundary-check/{divergence-allowlist.json,allowlist.json}; return per-identity evidence and required patches, never shared-file writes.

## Operator Guidance

Owner: `audit-build-route-assets`. Completion updates this plan and its exact br issue only after evidence is reviewed. Never mark a task complete because it is queued, drafted, or blocked.

Dependencies: [gate-baseline-freeze](um-parallel-20260909-gate-baseline-freeze.plan.md)

Resource locks: node-private mutable workspace and artifacts; apply the program’s host/build resource policy.

Verification: - Read-only audit may start immediately: regenerate owned identity/edge evidence against the selected integration head, fixed audited base eded841256a7cffdaa622e3889fc83407debd3e4 and actual complete PR merge range, including prior unmerged work. Classify fork additions versus audited identities; prove the native extension route or record the exact upstream blocker.

- After later implementation authorization, run focused existing behavior tests plus the listed real-output regressions, record deletion/move/retention and complete-PR/cumulative added-plus-removed line and hunk counts, and hand integration the evidence for the full canonical divergence/import gates. Moving an audited identity never resets its cap.

- Compile actual route assets for normal/automatic publicPath, hashed/unhashed assets and browser/server/worker outputs; verify RSC-disabled client/server entrypoints fail with the intended error while enabled RSC behavior remains native. Run routerPlugin and RSC tests under the new owner and relevant existing analyze tests.

Stop condition: All 12 reserved identities have an evidence-backed legal disposition and preserved route/RSC output; unlanded upstream seam requirements remain explicit blockers. No route-manifest rewrite or consumer navigation wrapper hides a defect.

Dispatch ready nodes continuously, up to 40 active leaf workers under the resolved 50-thread budget. Use immediate per-node handoffs; do not wait for an entire conceptual wave. The primary owns the shared integration service and exact source/receipt reconciliation.
