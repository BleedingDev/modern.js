---
name: um-parallel-20260909-gate-published-tractor
overview: "Run the Tractor acceptance using exact registry-installed bytes and the prepared canonical driver/fixture contract. Preserve native behavior and all consumer-owned UI/configuration."
todos:
  - id: gate-published-tractor-deliver
    content: "Run the Tractor acceptance using exact registry-installed bytes and the prepared canonical driver/fixture contract. Preserve native behavior and all consumer-owned UI/configuration."
    status: pending
  - id: gate-published-tractor-prove
    content: "Produce and verify: One complete published Tractor Node/workerd/UI receipt bound to the exact producer and registry digests."
    status: pending
isProject: false
---

# Verify Tractor from exact published bytes

## Execution Notes

Tracking: `modernjs-cdhz.29.116`. Mode: **independent published acceptance lane; separate private consumers and resource leases**. Read [the parallel program](um-parallel-20260909-overview.md) and [the unchanged acceptance contract](um-zero-20260909-overview.md). This is a deferred execution plan; creating it does not perform its tasks.

Run the Tractor acceptance using exact registry-installed bytes and the prepared canonical driver/fixture contract. Preserve native behavior and all consumer-owned UI/configuration.

Required output: One complete published Tractor Node/workerd/UI receipt bound to the exact producer and registry digests.

External prerequisites:

- Canonical Tractor existence and exclusive repository ownership are verified; a present busy canonical repository waits for its lease.

- Supported runtime environment lease; shared host acquires host-default-runtime-ports.

## Constraints

You are not alone in the codebase. Preserve other owners’ changes. Every worker is a leaf; only the primary agent schedules new nodes. Current planning authorization grants no framework changes or release.

Reserved file scope:

- `/Users/satan/side/experiments/tractor-store-vertical`

Out of scope: No unauthorized live deployment, fixture substitution for a present required canonical Tractor repository, app workaround or source-only pass.

Exact new destinations, source-barrel transfers and consumed interface deliveries must be recorded before writes. An audit report containing unresolved rows is not a completed route gate. Required upstream seams become explicit per-seam graph nodes/edges; an unmerged proposal cannot satisfy a landing prerequisite. Revalidate the exact selection after any expansion.

Keep fixed audited identity and full canonical scope, the complete-PR 20-line cap per audited file, required same-PR ledger and native extension ownership. No allowance reset, hidden imports, marker/identity renaming, app shim, copied framework wrapper, generated-file hand edit or hook bypass.

## Operator Guidance

Owner: `gate-published-tractor`. Completion updates this plan and its exact br issue only after evidence is reviewed. Never mark a task complete because it is queued, drafted, or blocked.

Dependencies: [gate-publication](um-parallel-20260909-gate-publication.plan.md)

Resource locks: `host-default-runtime-ports`, `canonical-tractor`

Verification: Install exact registry bytes; verify complete Tractor receipt, native Node/workerd behavior, shopping/search/API/SSR and preserved intended visuals; release only owned resources.

Stop condition: Unreserved consumer, manual repair, UI/runtime regression, mismatched cohort or incomplete published receipts blocks closeout.

Dispatch ready nodes continuously, up to 40 active leaf workers under the resolved 50-thread budget. Use immediate per-node handoffs; do not wait for an entire conceptual wave. The primary owns the shared integration service and exact source/receipt reconciliation.
