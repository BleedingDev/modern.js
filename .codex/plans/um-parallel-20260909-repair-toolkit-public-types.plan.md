---
name: um-parallel-20260909-repair-toolkit-public-types
overview: "Keep CLI route declarations and published declaration resolution complete without inventing alternate native router or Effect API identities."
todos:
  - id: repair-toolkit-public-types-implement
    content: "Implement the accepted per-identity routes in the exact granted source and fork-owned destination files; remove duplicated old implementations and eliminate this shard’s governed import edges."
    status: pending
  - id: repair-toolkit-public-types-verify
    content: "Run focused behavior and negative regressions appropriate to this module, check complete-PR cap/ledger obligations, and distinguish locally proven behavior from final integrated artifact qualification."
    status: pending
  - id: repair-toolkit-public-types-handoff
    content: "Return reviewed source changes, exact shared manifest/export/ledger patches and identity accounting to their sole integration owner; unblock successors immediately without waiting for unrelated component workers."
    status: pending
isProject: false
---

# Repair: Native CLI route declarations and shipped reexports

## Execution Notes

Tracking: `modernjs-cdhz.29.70`. Mode: **write-capable**. Read [the parallel program](um-parallel-20260909-overview.md) and [the unchanged acceptance contract](um-zero-20260909-overview.md). This is a deferred execution plan; creating it does not perform its tasks.

Keep CLI route declarations and published declaration resolution complete without inventing alternate native router or Effect API identities.

Required output: Complete scoped patch plus identity/import disposition map, shared-file handoffs and concrete focused-test evidence.

Read-only input identities:

- `packages/toolkit/types/cli/index.d.ts`
- `packages/toolkit/types/tests/reexports.test.ts`
- `packages/toolkit/types/tests/tsconfig.json`

Local seam prerequisites:

- Independent read-only route audit starts after the actual integration baseline is available; it does not wait for other toolkit shard audits.

- Source writes await this shard's evidence-backed legal route and the specific native interface needed by each identity.

- Freeze the exact route/type contract with runtime-router and builder owners; type integration waits only for their accepted native declaration seam.

- Decide whether each CLI declaration delta is upstream-neutral and cap-eligible, an existing extension-point use, or requires actual upstream landing.

## Constraints

You are not alone in the codebase. Preserve other owners’ changes. Every worker is a leaf; only the primary agent schedules new nodes. Current planning authorization grants no framework changes or release.

Reserved file scope:

- `packages/toolkit/types/cli/index.d.ts`
- `packages/toolkit/types/tests/reexports.test.ts`
- `packages/toolkit/types/tests/tsconfig.json`

Prospective destinations (not write grants):

Reserve tests/toolkit-boundary/public-types/** for qualification of shipped native declarations; fork-specific declaration extensions may occupy only a types/cli-route-contract/** subtree in the actual fork owner chosen by the route audit. Do not take ownership of runtime-router source or regenerate audited CLI declarations under a new identity.

Out of scope: Other source reservations, shared manifests/lockfile/ledger/allowlists, unapproved destinations, upstream submission, consumer shims and generated output edits.

Exact new destinations, source-barrel transfers and consumed interface deliveries must be recorded before writes. An audit report containing unresolved rows is not a completed route gate. Required upstream seams become explicit per-seam graph nodes/edges; an unmerged proposal cannot satisfy a landing prerequisite. Revalidate the exact selection after any expansion.

Keep fixed audited identity and full canonical scope, the complete-PR 20-line cap per audited file, required same-PR ledger and native extension ownership. No allowance reset, hidden imports, marker/identity renaming, app shim, copied framework wrapper, generated-file hand edit or hook bypass.

Shared-owner handoffs:

- Runtime-router/build owners receive exact declaration signatures and consumer compile expectations; they own their consumer files.

- Packaging owns exports/types entrypoints and project-wide package references; this shard supplies the exact shipped declaration inventory and requested patches.

- Single packaging/integration owner writes package manifests/exports, pnpm-lock.yaml, package cohort, FORK-DIVERGENCE.md and both boundary allowance files; submit exact proposed changes and identity/cap evidence without writing those files. Changelogs remain docs-owned.

## Operator Guidance

Owner: `repair-toolkit-public-types`. Completion updates this plan and its exact br issue only after evidence is reviewed. Never mark a task complete because it is queued, drafted, or blocked.

Dependencies: [audit-toolkit-public-types](um-parallel-20260909-audit-toolkit-public-types.plan.md), [gate-shared-service](um-parallel-20260909-gate-shared-service.plan.md)

Resource locks: node-private mutable workspace and artifacts; apply the program’s host/build resource policy.

Verification: - Run the reexports regression over actual shipped .d.ts files with NodeNext module resolution, not only source project compilation.

- Compile representative supported native route declarations and preserve upstream/native package type identity; no duplicate router/Effect contract facade.

- Regenerate baseline-qualified per-identity evidence; report complete eventual-PR added-plus-removed lines against its actual PR base and cumulative hunks/lines against eded841256a7cffdaa622e3889fc83407debd3e4. Run canonical full-scope divergence and import gates through the integration owner; local checks are diagnostic, never a narrowed substitute.

Stop condition: Stop source writes for an identity whose native seam, genuine fork owner, or complete-PR <=20-line upstream-owned patch is unproved; supply an exact neutral upstream proposal and external dependency where needed. A rename retains audited ownership. No implementation deletion without preserved behavior and replacement proof; no marker hiding, allowance reset, wrapper or facade that retains the old subsystem.

Dispatch ready nodes continuously, up to 40 active leaf workers under the resolved 50-thread budget. Use immediate per-node handoffs; do not wait for an entire conceptual wave. The primary owns the shared integration service and exact source/receipt reconciliation.
