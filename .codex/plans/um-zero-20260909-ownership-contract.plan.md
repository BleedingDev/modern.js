---
name: um-zero-20260909-ownership-contract
overview: "Choose legal ownership routes and freeze extension interfaces. Per-file disposition register, package/public-interface map, shared-file reservations and upstream proposal contracts."
todos:
  - id: zero-ownership-contract-classify
    content: "Assign every debt file and all 21 current import edges a concrete retain-native, fork-owned extraction, supported extension point, genuine upstream fix, or legal capped-patch route with callers, destinations and test obligations."
    status: pending
  - id: zero-ownership-contract-seams
    content: "Freeze neutral host-to-plugin interfaces and package contracts that let fork owners depend on upstream APIs without upstream importing fork packages; list every absent seam as an upstream prerequisite."
    status: pending
  - id: zero-ownership-contract-packets
    content: "Split component ownership into independently reviewable file reservations, allocate complete-PR cap capacity across all lanes, and record unresolved routes as blocked work rather than allowance growth."
    status: pending
isProject: false
---

# Choose legal ownership routes and freeze extension interfaces

## Execution Notes

Tracking: `modernjs-cdhz.4`. Parent planning program: `modernjs-cdhz`. **Planning only: every implementation todo remains pending.** No subagents are launched by this plan. Read [the program contract](um-zero-20260909-overview.md) and [initial ownership coverage](notes/um-zero-20260909/ownership-coverage.md).

Reuse runtime-extensions, i18n-extensions, plugin-bff-extensions, bff-effect, server-runtime-extensions, app-tools-extensions, code-tools, ultramodern-create and the fork docs/Sandpack packages. A new package needs a demonstrated separate responsibility, not one package per violation. No copying audited identities under a new name to claim exemption. Tests and docs move only when fork-created; audited originals keep their identity. Generic upstream capabilities must be useful upstream, not loaders for a named fork subsystem.

## Constraints

Own: Per-file disposition register, package/public-interface map, shared-file reservations and upstream proposal contracts.

Keep AGENTS.md Rules 1–5, CONTEXT.md and ADR-0019. Additive fork behavior belongs in genuine fork-owned packages; audited identities retain their full-PR 20-line cap and required same-PR ledger. No allowance reset, scope narrowing, hidden import indirection, generated-output edit, app shim or hook bypass. Current planning permission does not authorize implementation, upstream submission, merge or release.

## Operator Guidance

Owner: **Architecture and integration owner**. You are not alone in the codebase: preserve other owners' work and do not edit outside your reservation. Shared manifest, export, lockfile and ledger changes go to integration owners. At execution, update these todo statuses and the br issue from actual evidence; never mark future work completed merely because its plan exists.

Dependencies: [um-zero-20260909-baseline](um-zero-20260909-baseline.plan.md)

Verification and exit: Every row has an owner and a justified legal route; unresolved feasibility is visible. No empty adapters, dynamic-import indirection, renamed markers, custom app navigation, or packaging aliases used to conceal edges.

Launch successors when their actual prerequisites finish. Do not impose a global wave or two-agent cap. Retain exact plan selection/dependencies and graph ID when later running plan-graph/subagent-graph/helm. Upstream wait states and shared ports are real constraints; elapsed time is not approval.
