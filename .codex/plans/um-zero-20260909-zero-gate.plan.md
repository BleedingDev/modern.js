---
name: um-zero-20260909-zero-gate
overview: "Prove zero canonical violations and zero governed import edges. Canonical gate, shrink-only divergence accounting, reviewed import allowance retirement and exact inventory reconciliation; uses existing issue modernjs-piop."
todos:
  - id: zero-zero-gate-complete
    content: "Resolve every original and newly discovered inventory row and reconcile all upstream/native/fork ownership routes against the final integrated source; no residual historical exceptions may be waived."
    status: pending
  - id: zero-zero-gate-lock
    content: "Run the full canonical checker and independently require zero current import edges, not merely zero added edges; retire stale import allowances and lock only genuine cumulative shrink through supported reviewed operations."
    status: pending
  - id: zero-zero-gate-enforce
    content: "Verify normal CLI and CI cannot pass any current edge via an allowance, alternate roots, aliases, marker renaming or ignored categories; require zero divergence violations, zero current imports, zero active/stale import exemptions and valid per-file caps/ledger."
    status: pending
isProject: false
---

# Prove zero canonical violations and zero governed import edges

## Execution Notes

Tracking: `modernjs-piop`. Parent planning program: `modernjs-cdhz`. **Planning only: every implementation todo remains pending.** No subagents are launched by this plan. Read [the program contract](um-zero-20260909-overview.md) and [initial ownership coverage](notes/um-zero-20260909/ownership-coverage.md).

Zero violations does not mean literally zero legitimate upstream diff: real neutral capped changes may remain within unchanged/lowered budgets with valid evidence. It does mean no additive fork subsystem in vanilla packages and no governed upstream-to-fork import edges. Preserve complete canonical scope, audited identities and exact ref validation. Do not mass-approve inherited excess or clear the ledger by changing a tag/base.

## Constraints

Own: Canonical gate, shrink-only divergence accounting, reviewed import allowance retirement and exact inventory reconciliation; uses existing issue modernjs-piop.

Keep AGENTS.md Rules 1–5, CONTEXT.md and ADR-0019. Additive fork behavior belongs in genuine fork-owned packages; audited identities retain their full-PR 20-line cap and required same-PR ledger. No allowance reset, scope narrowing, hidden import indirection, generated-output edit, app shim or hook bypass. Current planning permission does not authorize implementation, upstream submission, merge or release.

## Operator Guidance

Owner: **Boundary integration owner**. You are not alone in the codebase: preserve other owners' work and do not edit outside your reservation. Shared manifest, export, lockfile and ledger changes go to integration owners. At execution, update these todo statuses and the br issue from actual evidence; never mark future work completed merely because its plan exists.

Dependencies: [um-zero-20260909-packaging](um-zero-20260909-packaging.plan.md), [um-zero-20260909-retirement](um-zero-20260909-retirement.plan.md)

Verification and exit: Exact committed head/base reports with violations=0, current import edges=0 and empty obsolete import allowance set. Re-run negative gate fixtures and prove all 447 initial union identities are reconciled or correctly superseded by fresh inventory evidence.

Launch successors when their actual prerequisites finish. Do not impose a global wave or two-agent cap. Retain exact plan selection/dependencies and graph ID when later running plan-graph/subagent-graph/helm. Upstream wait states and shared ports are real constraints; elapsed time is not approval.
