---
name: um-parallel-20260909-gate-final-zero
overview: "Apply only the reviewed identity/budget-preserving upstream transition, retire active/stale import allowances after actual repairs, lock genuine shrink through supported operations, then commit with integration and run canonical checks on that resulting exact source ref."
todos:
  - id: gate-final-zero-deliver
    content: "Apply only the reviewed identity/budget-preserving upstream transition, retire active/stale import allowances after actual repairs, lock genuine shrink through supported operations, then commit with integration and run canonical checks on that resulting exact source ref."
    status: pending
  - id: gate-final-zero-prove
    content: "Produce and verify: Exact head/base reports with zero divergence violations, zero current governed import edges, zero active/stale import exemptions, and complete reconciliation of every initial/new debt identity."
    status: pending
isProject: false
---

# Apply reviewed accounting and prove complete canonical zero debt

## Execution Notes

Tracking: `modernjs-piop`. Mode: **single boundary-governance metadata owner followed by exact-ref verification**. Read [the parallel program](um-parallel-20260909-overview.md) and [the unchanged acceptance contract](um-zero-20260909-overview.md). This is a deferred execution plan; creating it does not perform its tasks.

Apply only the reviewed identity/budget-preserving upstream transition, retire active/stale import allowances after actual repairs, lock genuine shrink through supported operations, then commit with integration and run canonical checks on that resulting exact source ref.

Required output: Exact head/base reports with zero divergence violations, zero current governed import edges, zero active/stale import exemptions, and complete reconciliation of every initial/new debt identity.

## Constraints

You are not alone in the codebase. Preserve other owners’ changes. Every worker is a leaf; only the primary agent schedules new nodes. Current planning authorization grants no framework changes or release.

Reserved file scope:

- `scripts/ultramodern-boundary-check/divergence-allowlist.json`
- `scripts/ultramodern-boundary-check/allowlist.json`

Out of scope: No mass approval, budget reset, hiding via aliases/renames/dynamic import, silent classifier exception or blanket deletion of meaningful tests.

Exact new destinations, source-barrel transfers and consumed interface deliveries must be recorded before writes. An audit report containing unresolved rows is not a completed route gate. Required upstream seams become explicit per-seam graph nodes/edges; an unmerged proposal cannot satisfy a landing prerequisite. Revalidate the exact selection after any expansion.

Keep fixed audited identity and full canonical scope, the complete-PR 20-line cap per audited file, required same-PR ledger and native extension ownership. No allowance reset, hidden imports, marker/identity renaming, app shim, copied framework wrapper, generated-file hand edit or hook bypass.

## Operator Guidance

Owner: `gate-final-zero`. Completion updates this plan and its exact br issue only after evidence is reviewed. Never mark a task complete because it is queued, drafted, or blocked.

Dependencies: [gate-source-assembly](um-parallel-20260909-gate-source-assembly.plan.md)

Resource locks: node-private mutable workspace and artifacts; apply the program’s host/build resource policy.

Verification: Re-run negative checker fixtures and independent CI re-derivation. Resolve all 440 initial failures and all 21 current imports, not merely newly added edges; account for all 447 original union identities. Fixed audited base and full packages scope remain unchanged; legitimate capped neutral diffs may remain with required ledger.

Stop condition: Any residual current edge, unresolved historical identity, accounting growth disguised as shrink, invalid cap/ledger, alternate scope or unverified provenance blocks qualification.

Dispatch ready nodes continuously, up to 40 active leaf workers under the resolved 50-thread budget. Use immediate per-node handoffs; do not wait for an entire conceptual wave. The primary owns the shared integration service and exact source/receipt reconciliation.
