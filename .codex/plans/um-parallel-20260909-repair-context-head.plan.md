---
name: um-parallel-20260909-repair-context-head
overview: "Own the shared context object, extension-slot visibility, React provider wrapper and head collector edge as one identity-sensitive module; these files directly import each other."
todos:
  - id: repair-context-head-implement
    content: "Implement the accepted per-identity routes in the exact granted source and fork-owned destination files; remove duplicated old implementations and eliminate this shard’s governed import edges."
    status: pending
  - id: repair-context-head-verify
    content: "Run focused behavior and negative regressions appropriate to this module, check complete-PR cap/ledger obligations, and distinguish locally proven behavior from final integrated artifact qualification."
    status: pending
  - id: repair-context-head-handoff
    content: "Return reviewed source changes, exact shared manifest/export/ledger patches and identity accounting to their sole integration owner; unblock successors immediately without waiting for unrelated component workers."
    status: pending
isProject: false
---

# Repair: Runtime context identity and head collection

## Execution Notes

Tracking: `modernjs-cdhz.29.2`. Mode: **write-capable**. Read [the parallel program](um-parallel-20260909-overview.md) and [the unchanged acceptance contract](um-zero-20260909-overview.md). This is a deferred execution plan; creating it does not perform its tasks.

Own the shared context object, extension-slot visibility, React provider wrapper and head collector edge as one identity-sensitive module; these files directly import each other.

Required output: Complete scoped patch plus identity/import disposition map, shared-file handoffs and concrete focused-test evidence.

Read-only input identities:

- `packages/runtime/plugin-runtime/src/core/context/extensions.ts`
- `packages/runtime/plugin-runtime/src/core/context/helmetContext.ts`
- `packages/runtime/plugin-runtime/src/core/context/public.ts`
- `packages/runtime/plugin-runtime/src/core/context/runtime.ts`
- `packages/runtime/plugin-runtime/src/core/react/wrapper.tsx`
- `packages/runtime/plugin-runtime/src/exports/head.ts`
- `packages/runtime/plugin-runtime/tests/core/context/extensions.test.ts`
- `packages/runtime/plugin-runtime/tests/core/react/wrapper.test.tsx`
- `packages/runtime/plugin-runtime/tests/exports/head.test.tsx`

Local seam prerequisites:

- Read-only route audit can start immediately from recorded identities; reconcile against the selected integration head before accepting file dispositions. Source writes require this shard's own evidence-backed lawful route and exact source/destination reservation, not completion of a global ownership wave.

- Prove whether existing RuntimePlugin wrapping/SSR collector hooks can host the context and head behavior without a governed upstream-to-fork import. The current symbol slot is an implementation, not an already approved upstream interface.

- Agree only the extension accessor and context projection contract needed by router-realms-prefetch; agree head-record/Helmet state and RSC serialization contract with runtime-ssr. Independent context/head auditing needs neither lane to finish.

- If audited runtime.ts, wrapper.tsx or import-only exports/head.ts cannot meet the complete-PR 20 added-plus-removed line cap, prepare a neutral upstream proposal and record the external landing dependency; do not rename the identity or retain a concealed import.

## Constraints

You are not alone in the codebase. Preserve other owners’ changes. Every worker is a leaf; only the primary agent schedules new nodes. Current planning authorization grants no framework changes or release.

Reserved file scope:

- `packages/runtime/plugin-runtime/src/core/context/extensions.ts`
- `packages/runtime/plugin-runtime/src/core/context/helmetContext.ts`
- `packages/runtime/plugin-runtime/src/core/context/public.ts`
- `packages/runtime/plugin-runtime/src/core/context/runtime.ts`
- `packages/runtime/plugin-runtime/src/core/react/wrapper.tsx`
- `packages/runtime/plugin-runtime/src/exports/head.ts`
- `packages/runtime/plugin-runtime/tests/core/context/extensions.test.ts`
- `packages/runtime/plugin-runtime/tests/core/react/wrapper.test.tsx`
- `packages/runtime/plugin-runtime/tests/exports/head.test.tsx`

Prospective destinations (not write grants):

Fork additions belong in runtime-extensions behind a proved native runtime/React seam. Preserve the public RuntimeContext object identity. This shard owns the inventory export identity src/exports/head.ts, including removal of its @modern-js/runtime-extensions edge; packaging applies resulting package-export changes.

Out of scope: Other source reservations, shared manifests/lockfile/ledger/allowlists, unapproved destinations, upstream submission, consumer shims and generated output edits.

Exact new destinations, source-barrel transfers and consumed interface deliveries must be recorded before writes. An audit report containing unresolved rows is not a completed route gate. Required upstream seams become explicit per-seam graph nodes/edges; an unmerged proposal cannot satisfy a landing prerequisite. Revalidate the exact selection after any expansion.

Keep fixed audited identity and full canonical scope, the complete-PR 20-line cap per audited file, required same-PR ledger and native extension ownership. No allowance reset, hidden imports, marker/identity renaming, app shim, copied framework wrapper, generated-file hand edit or hook bypass.

Shared-owner handoffs:

- packages/runtime/plugin-runtime/src/exports/head.ts

- packages/runtime/plugin-runtime/src/core/context/index.ts

- packages/runtime/plugin-runtime/src/router/runtime/lifecycle.ts

- packages/runtime/plugin-runtime/src/core/server/helmet.ts

- packages/runtime/plugin-runtime/src/core/server/string/index.ts

- packages/runtime/plugin-runtime/package.json

- packages/runtime/runtime-extensions/package.json

- packages/runtime/plugin-tanstack/package.json

- pnpm-lock.yaml

- FORK-DIVERGENCE.md

- scripts/ultramodern-boundary-check/divergence-allowlist.json

- scripts/ultramodern-boundary-check/allowlist.json

## Operator Guidance

Owner: `repair-context-head`. Completion updates this plan and its exact br issue only after evidence is reviewed. Never mark a task complete because it is queued, drafted, or blocked.

Dependencies: [audit-context-head](um-parallel-20260909-audit-context-head.plan.md), [gate-shared-service](um-parallel-20260909-gate-shared-service.plan.md)

Resource locks: node-private mutable workspace and artifacts; apply the program’s host/build resource policy.

Verification: - Run context extension, wrapper and head suites after lawful relocation. Preserve cross-copy accessor coherence, per-request isolation, no internal state in public enumeration/serialization, stable React context identity and server/client head behavior.

- Pair with SSR collector owner for stream/string and RSC coverage. Scan both direct and re-export dependencies; exports/head must have zero governed edges.

Stop condition: All nine reserved identities have proved disposition and tests remain exercised; stop this shard if the public-context or collector seam requires unresolved upstream changes. Other workers can continue.

Dispatch ready nodes continuously, up to 40 active leaf workers under the resolved 50-thread budget. Use immediate per-node handoffs; do not wait for an entire conceptual wave. The primary owns the shared integration service and exact source/receipt reconciliation.
