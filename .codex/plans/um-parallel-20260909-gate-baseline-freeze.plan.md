---
name: um-parallel-20260909-gate-baseline-freeze
overview: "Freeze the execution checkout, eventual combined-PR base, source SHA, audited base eded841256a7cffdaa622e3889fc83407debd3e4, reviewed upstream ref, canonical packages scope and allowance hashes. Generate raw complete divergence/import inventories without waiting for every legal design."
todos:
  - id: gate-baseline-freeze-deliver
    content: "Freeze the execution checkout, eventual combined-PR base, source SHA, audited base eded841256a7cffdaa622e3889fc83407debd3e4, reviewed upstream ref, canonical packages scope and allowance hashes. Generate raw complete divergence/import inventories without waiting for every legal design."
    status: pending
  - id: gate-baseline-freeze-prove
    content: "Produce and verify: Immutable ref/hash tuple plus complete machine-readable raw inventories for independently allocated audits."
    status: pending
isProject: false
---

# Pin the integration refs and canonical inventory inputs

## Execution Notes

Tracking: `modernjs-cdhz.29.99`. Mode: **single-owner read-only root**. Read [the parallel program](um-parallel-20260909-overview.md) and [the unchanged acceptance contract](um-zero-20260909-overview.md). This is a deferred execution plan; creating it does not perform its tasks.

Freeze the execution checkout, eventual combined-PR base, source SHA, audited base eded841256a7cffdaa622e3889fc83407debd3e4, reviewed upstream ref, canonical packages scope and allowance hashes. Generate raw complete divergence/import inventories without waiting for every legal design.

Required output: Immutable ref/hash tuple plus complete machine-readable raw inventories for independently allocated audits.

External prerequisites:

- external: execution has been authorized and the isolated execution checkout is reserved

## Constraints

You are not alone in the codebase. Preserve other owners’ changes. Every worker is a leaf; only the primary agent schedules new nodes. Current planning authorization grants no framework changes or release.

No framework source write grant. Evidence uses a node-private file or artifact root assigned by the operator.

Out of scope: No source edits, upstream submission, allowance writers or full-program design barrier.

Exact new destinations, source-barrel transfers and consumed interface deliveries must be recorded before writes. An audit report containing unresolved rows is not a completed route gate. Required upstream seams become explicit per-seam graph nodes/edges; an unmerged proposal cannot satisfy a landing prerequisite. Revalidate the exact selection after any expansion.

Keep fixed audited identity and full canonical scope, the complete-PR 20-line cap per audited file, required same-PR ledger and native extension ownership. No allowance reset, hidden imports, marker/identity renaming, app shim, copied framework wrapper, generated-file hand edit or hook bypass.

## Operator Guidance

Owner: `gate-baseline-freeze`. Completion updates this plan and its exact br issue only after evidence is reviewed. Never mark a task complete because it is queued, drafted, or blocked.

Dependencies: independent root after execution authorization

Resource locks: node-private mutable workspace and artifacts; apply the program’s host/build resource policy.

Verification: Resolve every ref; reconcile PR49/intervening fork-main work; preserve 440 initial failures, 21 current edges and 447 union identities as reconciliation anchors rather than forcing stale counts.

Stop condition: Unresolved refs, unknown combined-PR base, mutable/unowned checkout or canonical input discrepancy remains explicit; do not manufacture a fresh 20-line budget.

Dispatch ready nodes continuously, up to 40 active leaf workers under the resolved 50-thread budget. Use immediate per-node handoffs; do not wait for an entire conceptual wave. The primary owns the shared integration service and exact source/receipt reconciliation.
