---
name: um-parallel-20260909-gate-published-erp
overview: "Clean-install exact published cohort and run full ERP-10 Node/workerd checks including exports/API/singletons and operational independence."
todos:
  - id: gate-published-erp-deliver
    content: "Clean-install exact published cohort and run full ERP-10 Node/workerd checks including exports/API/singletons and operational independence."
    status: pending
  - id: gate-published-erp-prove
    content: "Produce and verify: Authenticated published ERP receipt tied to registry digests/source/version/producer."
    status: pending
isProject: false
---

# Verify ERP from exact registry-installed bytes

## Execution Notes

Tracking: `modernjs-cdhz.29.107`. Mode: **independent published acceptance job; supported runtime environment lease**. Read [the parallel program](um-parallel-20260909-overview.md) and [the unchanged acceptance contract](um-zero-20260909-overview.md). This is a deferred execution plan; creating it does not perform its tasks.

Clean-install exact published cohort and run full ERP-10 Node/workerd checks including exports/API/singletons and operational independence.

Required output: Authenticated published ERP receipt tied to registry digests/source/version/producer.

External prerequisites:

- external: exclusive supported runtime environment; shared host acquires the default-runtime-ports mutex

## Constraints

You are not alone in the codebase. Preserve other owners’ changes. Every worker is a leaf; only the primary agent schedules new nodes. Current planning authorization grants no framework changes or release.

No framework source write grant. Evidence uses a node-private file or artifact root assigned by the operator.

Out of scope: No packed/source substitution, duplicate complete platform-labeled drivers or concurrent use of Tractor's ports.

Exact new destinations, source-barrel transfers and consumed interface deliveries must be recorded before writes. An audit report containing unresolved rows is not a completed route gate. Required upstream seams become explicit per-seam graph nodes/edges; an unmerged proposal cannot satisfy a landing prerequisite. Revalidate the exact selection after any expansion.

Keep fixed audited identity and full canonical scope, the complete-PR 20-line cap per audited file, required same-PR ledger and native extension ownership. No allowance reset, hidden imports, marker/identity renaming, app shim, copied framework wrapper, generated-file hand edit or hook bypass.

## Operator Guidance

Owner: `gate-published-erp`. Completion updates this plan and its exact br issue only after evidence is reviewed. Never mark a task complete because it is queued, drafted, or blocked.

Dependencies: [gate-publication](um-parallel-20260909-gate-publication.plan.md)

Resource locks: `host-default-runtime-ports`

Verification: Actual installed tarballs match published identity; all required driver checks pass on both runtimes and owned resources are released.

Stop condition: Registry inconsistency, incomplete checks or receipt identity mismatch blocks closeout.

Dispatch ready nodes continuously, up to 40 active leaf workers under the resolved 50-thread budget. Use immediate per-node handoffs; do not wait for an entire conceptual wave. The primary owns the shared integration service and exact source/receipt reconciliation.
