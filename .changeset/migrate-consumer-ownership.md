---
'@modern-js/ultramodern-create': patch
---

Update every declared package in the authenticated framework cohort during migration. Preserve authored tooling and deployment artifacts, custom federation composition, topology additions, and compatible newer pnpm versions. Refresh generated validators only when their executable source matches the framework template.
