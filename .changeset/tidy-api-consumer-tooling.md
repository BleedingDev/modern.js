---
'@modern-js/code-tools': patch
---

Package MicroVertical API consumer boundary validation with bounded Babel analysis, installed owner/export identity checks, and separate full and consumer-files CLI phases. Distinguish consumer diagnostics from tool failures and preserve existing operation IDs by checking the endpoints actually composed into each consumer API.
