---
'@modern-js/plugin-bff-extensions': patch
'@modern-js/code-tools': patch
---

Let Node 26 load installed ESM dependencies natively from compiled CommonJS API entries, preserving Effect schema identity between bundled workspace contracts and the BFF runtime.

Keep analyzer infrastructure error details visible in CI by selecting Oxlint's diagnostic output format explicitly.
