---
'@modern-js/ultramodern-create': patch
---

Include and install Node ambient types explicitly in generated TypeScript 7 workspaces, retain consumer type additions during migration, and check every referenced package from the root typecheck command.

Validate optional sample endpoint metadata when present without requiring consumer business APIs to retain the scaffold's sample endpoints.
