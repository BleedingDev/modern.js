---
'@modern-js/app-tools-extensions': patch
---

Build referenced TypeScript projects before checking an app without emitting its output, so clean builds and changed shared contracts do not fail on missing or stale declarations.
