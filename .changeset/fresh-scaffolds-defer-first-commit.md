---
"@modern-js/ultramodern-create": patch
---

Initialize new scaffold repositories without staging or committing before dependencies are installed. Show explicit first-commit steps after installation and checks so Git uses the user's identity and configured hooks. Preserve existing repositories and their staged changes.
