---
'@modern-js/create': patch
'@modern-js/sandpack-react': patch
---

Restore the native application template's Biome configuration and CSS to match its declared Biome version. Render the configuration through the existing template mechanism while preserving its exclusion from subprojects.
