---
'@modern-js/ultramodern-create': patch
---

Generate MicroVertical foundation imports from the native bff-effect package and run the packaged API checks. Combined workspace checks run topology once while standalone API checks retain full coverage.
